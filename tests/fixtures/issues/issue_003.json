{
    "issue_url": "https://github.com/acme/api-gateway/issues/7",
    "issue_title": "JWT tokens not invalidated on logout",
    "issue_body": "After calling /auth/logout, the JWT token remains valid until expiry. No token blacklist or revocation mechanism is implemented, violating security requirements.",
    "repository": "acme/api-gateway"
}
